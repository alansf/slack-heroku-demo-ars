{
  "mcpServers": {
    "mcp-applink": {
      "type": "stdio",
      "command": "mcp-applink",
      "args": [],
      "env": {
        "HEROKU_API_KEY": "${HEROKU_API_KEY}",
        "HEROKU_DYNO_ID": "${HEROKU_DYNO_ID}",
        "SALESFORCE_ORG_DOMAIN_URL": "${SALESFORCE_ORG_DOMAIN_URL}",
        "SALESFORCE_ORG_ID": "${SALESFORCE_ORG_ID}",
        "SALESFORCE_TENANT_GUID": "${SALESFORCE_TENANT_GUID}",
        "SALESFORCE_USER_INFO": "${SALESFORCE_USER_INFO}"
      }
    }
  }
}